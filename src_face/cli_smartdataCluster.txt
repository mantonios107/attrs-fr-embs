### Install environment on SmartData cluster

pip install pyarrow; pip uninstall pandas  -y; pip install pandas; pip install numexpr --upgrade

# for retina
pip install pyarrow pandas ; pip install onnxruntime-gpu; pip install insightface

# for facenet
pip install pyarrow pandas ; pip install keras-facenet ; pip install scipy; pip install tensorflow  # does not work with GPU

# for postprocessing
pip install -q pyarrow faiss-cpu

# for postprocessing metadata (create df_identities and df_attributes)
